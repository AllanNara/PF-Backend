SuccessfulCartOperation:
  description: Successful Cart Operation
  content:
    application/json:
      schema:
        allOf:
          - $ref: ../schemas/SuccessRequest.yaml
          - type: object
            properties:
              payload:
                $ref: ../schemas/Cart.yaml

SuccessfulProductOperation:
  description: Successful Product Operation
  content:
    application/json:
      schema:
        allOf:
          - $ref: ../schemas/SuccessRequest.yaml
          - type: object
            properties:
              payload:
                $ref: ../schemas/Product.yaml
      examples:
        AutoincID:
          $ref: ../examples/ProductExample.yaml#/Product_AutoincID
        MongoID:
          $ref: ../examples/ProductExample.yaml#/Product_MongoID
