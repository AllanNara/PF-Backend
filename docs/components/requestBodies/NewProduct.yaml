required: true
description: Fields to complete for new product, code must unique
content:
  application/json:
    schema:
      $ref: ../schemas/Product.yaml
      required:
        - title
        - description
        - code
        - price
        - stock
        - category
    examples:
      ValuesForm:
        $ref: ../examples/ProductExample.yaml#/FormAddProduct
